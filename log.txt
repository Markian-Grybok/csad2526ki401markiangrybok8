Step1:
- Напиши мені мінімальну програму "Hello World"(main.cpp).

Step2:
- Тепер  згенеруй наповнення до файлу CMakeLists.txt.

Step3:
- У math_operations.h створи функцію int add(int a, int b);
- Реалізуй функцію в math_operations.cpp.
- Напиши юніт тести unit_tests.cpp.
- Додай підтримку Google Test у CMakeLists.txt і додай інструкції для створення тестового виконуваного файлу та запуску тестів.

Step4:
- Створи скрипт ci.bat/ci.cmd (для Windows).
  Команди:
  1 Створення каталогу білдування (mkdir build).
  2 Перехід у каталог (cd build).
  3 Конфігурування проекту за допомогою CMake (cmake ..).
  4 Білдування проєкту (cmake --build .).
  5 Забезпечте права на виконання (chmod +x build.sh) та зафіксуйте
  зміни.
  6 Створіть крок, який запускає тести за допомогою CTest (частина CMake),
  і переконайтеся, що він виконується на всіх трьох ОС:

Step5:
- Напиши мені файл ci.yml  для
  генерації робочого процесу (workflow) з наступними вимогами:
  Trigger: Запускати акцію при push або pull request у гілку, що містить в
  назві develop чи master .
  Job: Створити одну Job (наприклад, cross_build).
  Strategy: Використати матрицю (strategy: matrix) для запуску білду
  на трьох ОС:runs-on: ubuntu-latest (Linux), runs-on: windows-latest (Windows), runs-on: macos-latest (macOS)
  Steps: Кожен білд повинен включати кроки: Checkout коду (actions/checkout@v4), Встановлення CMake (якщо потрібно),

Step6:
- Створи покращений CI/CD пайплайн для мого C++ проекту з математичними операціями.
  Проект використовує CMake для збірки та Google Test для модульного тестування.
  Потрібно забезпечити стабільну роботу на трьох операційних системах: Windows, Linux та macOS.
  Основна проблема полягає в тому, що тести не запускаються коректно через помилки шляхів до заголовкових файлів та проблеми з конфігурацією на різних платформах.     Потрібно виправити ці помилки та налаштувати пайплайн так, щоб збірка та тестування автоматично виконувались успішно при кожному коміті чи пул реквесті.
  Також важливо мати зрозумілі логи та артефакти збірки для кожної платформи.
